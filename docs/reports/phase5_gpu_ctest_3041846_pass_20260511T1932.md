# Phase 5 GPU CTest pass — job 3041846

Date: 2026-05-11 19:32 CEST
Lane: `g4gpu-phase5`
Branch: `lane/g4gpu-phase5`

## Summary

The full Phase 5 CTest gate that was previously pending on the LUNARC GPU
queue has completed successfully. This closes the GPU-allocation verification
blocker for the 5a--5c measurement framework, but it does **not** authorize
Phase 5d L0 optimization work by itself. The lane stop condition still requires
planner review and publication closure before touching 5d target files.

## SLURM evidence

Command used for the final scheduler audit:

```bash
sacct -X -j 3041846 --format=JobID,State,ExitCode,Elapsed,Timelimit,Start,End,NodeList -P
```

Observed result:

```text
JobID|State|ExitCode|Elapsed|Timelimit|Start|End|NodeList
3041846|COMPLETED|0:0|00:00:09|00:05:00|2026-05-11T19:32:35|2026-05-11T19:32:44|cg05
```

Output transcript retained in the worktree build area:

```text
/projects/hep/fs10/shared/nnbar/billy/geant4-gpu/build/g4gpu_phase5_ctest_current_3041846.out
```

## Build and test evidence

The job configured the existing build tree, rebuilt the Phase 5 targets, and ran
all registered tests under `build/`. The transcript records these build targets:

- `G4GPU`
- `test_stub`
- `test_voxel_geometry`
- `test_muon_range`
- `test_mcs`
- `test_benchmark_manifest`
- `benchmark_gamma_100mev`
- `benchmark_muon_10gev`
- `benchmark_nbar_carbon`
- `benchmark_cosmic_shower`
- `benchmark_optical_scintillator`
- `benchmark_beam_neutron`

CTest result:

```text
1/13  g4gpu_stub                                      Passed
2/13  g4gpu_voxel_geometry                            Passed
3/13  g4gpu_muon_range                                Passed
4/13  g4gpu_mcs                                       Passed
5/13  g4gpu_benchmark_manifest                        Passed
6/13  g4gpu_benchmark_script_syntax                   Passed
7/13  g4gpu_validate_harness                          Passed
8/13  g4gpu_benchmark_gamma_100mev_smoke              Passed
9/13  g4gpu_benchmark_muon_10gev_smoke                Passed
10/13 g4gpu_benchmark_nbar_carbon_smoke               Passed
11/13 g4gpu_benchmark_cosmic_shower_smoke             Passed
12/13 g4gpu_benchmark_optical_scintillator_smoke      Passed
13/13 g4gpu_benchmark_beam_neutron_smoke              Passed

100% tests passed, 0 tests failed out of 13
Total Test time (real) = 6.86 sec
```

## Gate status after this pass

Now green:

- full GPU-node CTest job `3041846`: `COMPLETED`, exit `0:0`, `13/13` tests
  passed;
- 5a benchmark suite implementation and LUNARC baseline artifacts;
- 5b CPU profiling artifacts;
- 5c validation harness and self-comparison artifacts;
- focused non-device measurement gate;
- credential-free local mirror/bundle/patch fallback at the publication path.

Still not complete:

- direct upstream GitHub push is not yet proven for this pane; if upstream push
  remains denied, use the configured fork remote and a draft pull request;
- planner review must explicitly clear the 5a--5c stop condition;
- 5d.1--5d.4 L0 microarchitecture work is intentionally unstarted;
- no speedup claim exists because no 5d optimization has run.

## Guardrail

Do not edit `src/physics/CrossSectionInterpolator.cc`,
`src/geometry/branchless_solids.cc`, `include/g4gpu/Track.hh`, or navigation
prefetch code until planner review/publication closure explicitly authorizes
Phase 5d.
